name: klimbb148
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flex_color_picker: ^3.3.0
  flutter:
    sdk: flutter
  geocode: ^1.0.3
  hive: ^2.2.3
  provider: ^6.0.5

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
