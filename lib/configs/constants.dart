import 'package:flutter/material.dart';

BorderRadiusGeometry borderRadius3 = const BorderRadius.only(
  topLeft: Radius.circular(10),
  topRight: Radius.circular(10),
  bottomLeft: Radius.circular(10),
);
